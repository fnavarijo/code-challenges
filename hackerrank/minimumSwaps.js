function minimumSwaps(arr) {
    // TODO: Finish this f**** challenge
    let minSwaps = 0;
    for(let i = 0; i < arr.length; i++) {
        let minRightSide = -1;
        for(let j = arr.length - 1; j >= 0; j--) {
            
            console.log(arr[i]);

        }
    }
    return minSwaps;
}

console.log(minimumSwaps([4, 3, 1, 2]));